export type TunnellerData = {
    id: string,
    forename: string,
    surname: string,
    birthYear: string | null,
    deathYear: string | null,
}

export type Tunneller = {
    id: string,
    forename: string,
    surname: string,
    birthYear: string | null,
    deathYear: string | null,
    fullName: string,
}
